CREATE TABLE DeprioritizedItems (
    id TEXT NOT NULL
);

insert:
INSERT OR REPLACE INTO DeprioritizedItems (
    id
) VALUES (
    :id
);

select:
SELECT id FROM DeprioritizedItems;

delete:
DELETE FROM DeprioritizedItems WHERE id = :id;
